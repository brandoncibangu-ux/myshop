{% doc %}
  @prompt
    generate for me a video slider carousel for tiktok format videos., create video slider for small video in ration 9:16 and no mobile slider so i want to put 3 video directly on the same section , horisontally
{% enddoc %}
{% assign ai_gen_id = block.id | replace: '_', '' | downcase %}

{% style %}
  .ai-video-grid-{{ ai_gen_id }} {
    display: block;
    width: 100%;
    background-color: {{ block.settings.background_color }};
    padding: {{ block.settings.padding_vertical }}px 0;
  }

  .ai-video-grid__container-{{ ai_gen_id }} {
    max-width: {{ block.settings.max_width }}px;
    margin: 0 auto;
    padding: 0 20px;
  }

  .ai-video-grid__header-{{ ai_gen_id }} {
    text-align: {{ block.settings.text_alignment }};
    margin-bottom: 30px;
  }

  .ai-video-grid__title-{{ ai_gen_id }} {
    font-size: {{ block.settings.title_size }}px;
    color: {{ block.settings.title_color }};
    margin: 0 0 10px;
  }

  .ai-video-grid__subtitle-{{ ai_gen_id }} {
    font-size: {{ block.settings.subtitle_size }}px;
    color: {{ block.settings.subtitle_color }};
    margin: 0;
  }

  .ai-video-grid__slider-wrapper-{{ ai_gen_id }} {
    position: relative;
    overflow: hidden;
  }

  .ai-video-grid__slider-{{ ai_gen_id }} {
    display: flex;
    gap: {{ block.settings.gap }}px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-behavior: smooth;
    scrollbar-width: none;
    -webkit-overflow-scrolling: touch;
  }

  .ai-video-grid__slider-{{ ai_gen_id }}::-webkit-scrollbar {
    display: none;
  }

  .ai-video-grid__slide-{{ ai_gen_id }} {
    flex: 0 0 auto;
    width: calc((100% - ({{ block.settings.gap }}px * 2)) / 3);
    scroll-snap-align: start;
    position: relative;
    background-color: {{ block.settings.video_background }};
    border-radius: {{ block.settings.video_border_radius }}px;
    overflow: hidden;
  }

  .ai-video-grid__video-wrapper-{{ ai_gen_id }} {
    position: relative;
    width: 100%;
    aspect-ratio: 9 / 16;
    background-color: #000000;
  }

  .ai-video-grid__video-{{ ai_gen_id }} {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .ai-video-grid__placeholder-{{ ai_gen_id }} {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    position: relative;
  }

  .ai-video-grid__placeholder-{{ ai_gen_id }} svg {
    width: 50px;
    height: 50px;
    opacity: 0.3;
    color: #ffffff;
  }

  .ai-video-grid__empty-state-{{ ai_gen_id }} {
    position: absolute;
    bottom: 15px;
    left: 50%;
    transform: translateX(-50%);
    color: #ffffff;
    font-size: 12px;
    text-align: center;
    padding: 0 10px;
  }

  .ai-video-grid__play-button-{{ ai_gen_id }} {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 50px;
    height: 50px;
    background-color: rgba(255, 255, 255, 0.9);
    border: none;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    z-index: 2;
  }

  .ai-video-grid__play-button-{{ ai_gen_id }}:hover {
    background-color: rgba(255, 255, 255, 1);
    transform: translate(-50%, -50%) scale(1.1);
  }

  .ai-video-grid__play-button-{{ ai_gen_id }} svg {
    width: 20px;
    height: 20px;
    margin-left: 2px;
  }

  .ai-video-grid__play-button-{{ ai_gen_id }}.playing {
    opacity: 0;
    pointer-events: none;
  }

  .ai-video-grid__info-{{ ai_gen_id }} {
    padding: 12px;
  }

  .ai-video-grid__video-title-{{ ai_gen_id }} {
    font-size: 14px;
    font-weight: 600;
    color: {{ block.settings.video_title_color }};
    margin: 0 0 5px;
  }

  .ai-video-grid__video-description-{{ ai_gen_id }} {
    font-size: 12px;
    color: {{ block.settings.video_description_color }};
    margin: 0;
    line-height: 1.4;
  }

  .ai-video-grid__controls-{{ ai_gen_id }} {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 15px;
    margin-top: 30px;
  }

  .ai-video-grid__nav-button-{{ ai_gen_id }} {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    background-color: {{ block.settings.nav_button_color }};
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
  }

  .ai-video-grid__nav-button-{{ ai_gen_id }}:hover:not(:disabled) {
    background-color: {{ block.settings.nav_button_hover_color }};
    transform: scale(1.1);
  }

  .ai-video-grid__nav-button-{{ ai_gen_id }}:disabled {
    opacity: 0.3;
    cursor: not-allowed;
  }

  .ai-video-grid__nav-button-{{ ai_gen_id }} svg {
    width: 20px;
    height: 20px;
    color: {{ block.settings.nav_button_icon_color }};
  }

  .ai-video-grid__dots-{{ ai_gen_id }} {
    display: flex;
    gap: 8px;
  }

  .ai-video-grid__dot-{{ ai_gen_id }} {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background-color: {{ block.settings.dot_color }};
    border: none;
    cursor: pointer;
    padding: 0;
    transition: all 0.3s ease;
  }

  .ai-video-grid__dot-{{ ai_gen_id }}.active {
    background-color: {{ block.settings.dot_active_color }};
    transform: scale(1.3);
  }

  @media screen and (max-width: 749px) {
    .ai-video-grid__slider-{{ ai_gen_id }} {
      overflow-x: visible;
      scroll-snap-type: none;
    }

    .ai-video-grid__slide-{{ ai_gen_id }} {
      width: calc((100% - ({{ block.settings.gap_mobile }}px * 2)) / 3);
    }

    .ai-video-grid__slider-wrapper-{{ ai_gen_id }} {
      overflow: visible;
    }

    .ai-video-grid__controls-{{ ai_gen_id }} {
      display: none;
    }

    .ai-video-grid__title-{{ ai_gen_id }} {
      font-size: {{ block.settings.title_size | times: 0.8 }}px;
    }

    .ai-video-grid__subtitle-{{ ai_gen_id }} {
      font-size: {{ block.settings.subtitle_size | times: 0.9 }}px;
    }

    .ai-video-grid__play-button-{{ ai_gen_id }} {
      width: 40px;
      height: 40px;
    }

    .ai-video-grid__play-button-{{ ai_gen_id }} svg {
      width: 16px;
      height: 16px;
    }

    .ai-video-grid__info-{{ ai_gen_id }} {
      padding: 8px;
    }

    .ai-video-grid__video-title-{{ ai_gen_id }} {
      font-size: 12px;
    }

    .ai-video-grid__video-description-{{ ai_gen_id }} {
      font-size: 11px;
    }
  }
{% endstyle %}

<video-grid-{{ ai_gen_id }}
  class="ai-video-grid-{{ ai_gen_id }}"
  {{ block.shopify_attributes }}
>
  <div class="ai-video-grid__container-{{ ai_gen_id }}">
    {% if block.settings.title != blank or block.settings.subtitle != blank %}
      <div class="ai-video-grid__header-{{ ai_gen_id }}">
        {% if block.settings.title != blank %}
          <h2 class="ai-video-grid__title-{{ ai_gen_id }}">{{ block.settings.title }}</h2>
        {% endif %}
        {% if block.settings.subtitle != blank %}
          <p class="ai-video-grid__subtitle-{{ ai_gen_id }}">{{ block.settings.subtitle }}</p>
        {% endif %}
      </div>
    {% endif %}

    <div class="ai-video-grid__slider-wrapper-{{ ai_gen_id }}">
      <div class="ai-video-grid__slider-{{ ai_gen_id }}" data-slider>
        {% for i in (1..6) %}
          {% liquid
            assign video_key = 'video_' | append: i
            assign video_title_key = 'video_title_' | append: i
            assign video_description_key = 'video_description_' | append: i
            assign video = block.settings[video_key]
            assign video_title = block.settings[video_title_key]
            assign video_description = block.settings[video_description_key]
          %}

          <div class="ai-video-grid__slide-{{ ai_gen_id }}" data-slide="{{ i }}">
            <div class="ai-video-grid__video-wrapper-{{ ai_gen_id }}">
              {% if video != blank %}
                {{ video | video_tag: autoplay: false, loop: true, controls: false, muted: false, class: 'ai-video-grid__video-' | append: ai_gen_id, image_size: '400x' }}
                <button
                  class="ai-video-grid__play-button-{{ ai_gen_id }}"
                  data-play-button
                  aria-label="Play video"
                >
                  <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M8 5v14l11-7z"/>
                  </svg>
                </button>
              {% else %}
                <div class="ai-video-grid__placeholder-{{ ai_gen_id }}">
                  <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z" />
                  </svg>
                  <div class="ai-video-grid__empty-state-{{ ai_gen_id }}">
                    Add video {{ i }}
                  </div>
                </div>
              {% endif %}
            </div>

            {% if video_title != blank or video_description != blank %}
              <div class="ai-video-grid__info-{{ ai_gen_id }}">
                {% if video_title != blank %}
                  <h3 class="ai-video-grid__video-title-{{ ai_gen_id }}">{{ video_title }}</h3>
                {% endif %}
                {% if video_description != blank %}
                  <p class="ai-video-grid__video-description-{{ ai_gen_id }}">{{ video_description }}</p>
                {% endif %}
              </div>
            {% endif %}
          </div>
        {% endfor %}
      </div>
    </div>

    {% if block.settings.show_controls %}
      <div class="ai-video-grid__controls-{{ ai_gen_id }}">
        <button
          class="ai-video-grid__nav-button-{{ ai_gen_id }}"
          data-prev
          aria-label="Previous videos"
        >
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
          </svg>
        </button>

        <div class="ai-video-grid__dots-{{ ai_gen_id }}" data-dots></div>

        <button
          class="ai-video-grid__nav-button-{{ ai_gen_id }}"
          data-next
          aria-label="Next videos"
        >
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
          </svg>
        </button>
      </div>
    {% endif %}
  </div>
</video-grid-{{ ai_gen_id }}>

<script>
  (function() {
    class VideoGrid{{ ai_gen_id }} extends HTMLElement {
      constructor() {
        super();
        this.currentPage = 0;
        this.totalSlides = 6;
        this.slidesPerPage = 3;
      }

      connectedCallback() {
        this.slider = this.querySelector('[data-slider]');
        this.slides = this.querySelectorAll('[data-slide]');
        this.prevButton = this.querySelector('[data-prev]');
        this.nextButton = this.querySelector('[data-next]');
        this.dotsContainer = this.querySelector('[data-dots]');
        this.videos = this.querySelectorAll('.ai-video-grid__video-{{ ai_gen_id }}');
        this.playButtons = this.querySelectorAll('[data-play-button]');

        this.setupDots();
        this.setupEventListeners();
        this.updateControls();
      }

      setupDots() {
        if (!this.dotsContainer) return;

        const totalPages = Math.ceil(this.totalSlides / this.slidesPerPage);
        
        for (let i = 0; i < totalPages; i++) {
          const dot = document.createElement('button');
          dot.className = 'ai-video-grid__dot-{{ ai_gen_id }}';
          if (i === 0) dot.classList.add('active');
          dot.setAttribute('aria-label', `Go to page ${i + 1}`);
          dot.addEventListener('click', () => this.goToPage(i));
          this.dotsContainer.appendChild(dot);
        }

        this.dots = this.querySelectorAll('.ai-video-grid__dot-{{ ai_gen_id }}');
      }

      setupEventListeners() {
        if (this.prevButton) {
          this.prevButton.addEventListener('click', () => this.prev());
        }

        if (this.nextButton) {
          this.nextButton.addEventListener('click', () => this.next());
        }

        this.playButtons.forEach((button, index) => {
          button.addEventListener('click', () => this.toggleVideo(index));
        });

        this.videos.forEach((video, index) => {
          video.addEventListener('click', () => this.toggleVideo(index));
          video.addEventListener('ended', () => {
            this.playButtons[index].classList.remove('playing');
          });
        });

        this.slider.addEventListener('scroll', () => {
          this.updateCurrentPage();
        });
      }

      toggleVideo(index) {
        const video = this.videos[index];
        const playButton = this.playButtons[index];

        if (!video) return;

        if (video.paused) {
          this.pauseAllVideos();
          video.play();
          playButton.classList.add('playing');
        } else {
          video.pause();
          playButton.classList.remove('playing');
        }
      }

      pauseAllVideos() {
        this.videos.forEach((video, index) => {
          video.pause();
          this.playButtons[index].classList.remove('playing');
        });
      }

      updateCurrentPage() {
        const scrollLeft = this.slider.scrollLeft;
        const slideWidth = this.slides[0].offsetWidth;
        const gap = {{ block.settings.gap }};
        const pageWidth = (slideWidth + gap) * this.slidesPerPage;
        this.currentPage = Math.round(scrollLeft / pageWidth);
        this.updateControls();
      }

      goToPage(page) {
        this.pauseAllVideos();
        const slideWidth = this.slides[0].offsetWidth;
        const gap = {{ block.settings.gap }};
        const pageWidth = (slideWidth + gap) * this.slidesPerPage;
        this.slider.scrollLeft = page * pageWidth;
        this.currentPage = page;
        this.updateControls();
      }

      prev() {
        if (this.currentPage > 0) {
          this.goToPage(this.currentPage - 1);
        }
      }

      next() {
        const totalPages = Math.ceil(this.totalSlides / this.slidesPerPage);
        if (this.currentPage < totalPages - 1) {
          this.goToPage(this.currentPage + 1);
        }
      }

      updateControls() {
        const totalPages = Math.ceil(this.totalSlides / this.slidesPerPage);

        if (this.prevButton) {
          this.prevButton.disabled = this.currentPage === 0;
        }

        if (this.nextButton) {
          this.nextButton.disabled = this.currentPage === totalPages - 1;
        }

        if (this.dots) {
          this.dots.forEach((dot, index) => {
            if (index === this.currentPage) {
              dot.classList.add('active');
            } else {
              dot.classList.remove('active');
            }
          });
        }
      }
    }

    customElements.define('video-grid-{{ ai_gen_id }}', VideoGrid{{ ai_gen_id }});
  })();
</script>

{% schema %}
{
  "name": "Video grid slider",
  "tag": null,
  "settings": [
    {
      "type": "header",
      "content": "Heading"
    },
    {
      "type": "inline_richtext",
      "id": "title",
      "label": "Title",
      "default": "Watch our videos"
    },
    {
      "type": "inline_richtext",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Discover our latest content"
    },
    {
      "type": "range",
      "id": "title_size",
      "min": 20,
      "max": 60,
      "step": 2,
      "unit": "px",
      "label": "Title size",
      "default": 32
    },
    {
      "type": "range",
      "id": "subtitle_size",
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Subtitle size",
      "default": 16
    },
    {
      "type": "select",
      "id": "text_alignment",
      "label": "Text alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "center"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Title color",
      "default": "#1a1a1a"
    },
    {
      "type": "color",
      "id": "subtitle_color",
      "label": "Subtitle color",
      "default": "#666666"
    },
    {
      "type": "header",
      "content": "Video 1"
    },
    {
      "type": "video",
      "id": "video_1",
      "label": "Video"
    },
    {
      "type": "text",
      "id": "video_title_1",
      "label": "Title"
    },
    {
      "type": "textarea",
      "id": "video_description_1",
      "label": "Description"
    },
    {
      "type": "header",
      "content": "Video 2"
    },
    {
      "type": "video",
      "id": "video_2",
      "label": "Video"
    },
    {
      "type": "text",
      "id": "video_title_2",
      "label": "Title"
    },
    {
      "type": "textarea",
      "id": "video_description_2",
      "label": "Description"
    },
    {
      "type": "header",
      "content": "Video 3"
    },
    {
      "type": "video",
      "id": "video_3",
      "label": "Video"
    },
    {
      "type": "text",
      "id": "video_title_3",
      "label": "Title"
    },
    {
      "type": "textarea",
      "id": "video_description_3",
      "label": "Description"
    },
    {
      "type": "header",
      "content": "Video 4"
    },
    {
      "type": "video",
      "id": "video_4",
      "label": "Video"
    },
    {
      "type": "text",
      "id": "video_title_4",
      "label": "Title"
    },
    {
      "type": "textarea",
      "id": "video_description_4",
      "label": "Description"
    },
    {
      "type": "header",
      "content": "Video 5"
    },
    {
      "type": "video",
      "id": "video_5",
      "label": "Video"
    },
    {
      "type": "text",
      "id": "video_title_5",
      "label": "Title"
    },
    {
      "type": "textarea",
      "id": "video_description_5",
      "label": "Description"
    },
    {
      "type": "header",
      "content": "Video 6"
    },
    {
      "type": "video",
      "id": "video_6",
      "label": "Video"
    },
    {
      "type": "text",
      "id": "video_title_6",
      "label": "Title"
    },
    {
      "type": "textarea",
      "id": "video_description_6",
      "label": "Description"
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "max_width",
      "min": 600,
      "max": 1600,
      "step": 100,
      "unit": "px",
      "label": "Maximum width",
      "default": 1200
    },
    {
      "type": "range",
      "id": "gap",
      "min": 10,
      "max": 40,
      "step": 5,
      "unit": "px",
      "label": "Gap between videos (desktop)",
      "default": 20
    },
    {
      "type": "range",
      "id": "gap_mobile",
      "min": 5,
      "max": 20,
      "step": 5,
      "unit": "px",
      "label": "Gap between videos (mobile)",
      "default": 10
    },
    {
      "type": "range",
      "id": "padding_vertical",
      "min": 20,
      "max": 100,
      "step": 10,
      "unit": "px",
      "label": "Vertical padding",
      "default": 60
    },
    {
      "type": "header",
      "content": "Style"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "video_background",
      "label": "Video card background",
      "default": "#f5f5f5"
    },
    {
      "type": "range",
      "id": "video_border_radius",
      "min": 0,
      "max": 40,
      "step": 4,
      "unit": "px",
      "label": "Video border radius",
      "default": 16
    },
    {
      "type": "color",
      "id": "video_title_color",
      "label": "Video title color",
      "default": "#1a1a1a"
    },
    {
      "type": "color",
      "id": "video_description_color",
      "label": "Video description color",
      "default": "#666666"
    },
    {
      "type": "header",
      "content": "Controls"
    },
    {
      "type": "checkbox",
      "id": "show_controls",
      "label": "Show navigation controls",
      "info": "Controls are hidden on mobile",
      "default": true
    },
    {
      "type": "color",
      "id": "nav_button_color",
      "label": "Navigation button color",
      "default": "#f575b4"
    },
    {
      "type": "color",
      "id": "nav_button_hover_color",
      "label": "Navigation button hover color",
      "default": "#e05a9f"
    },
    {
      "type": "color",
      "id": "nav_button_icon_color",
      "label": "Navigation icon color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "dot_color",
      "label": "Dot color",
      "default": "#cccccc"
    },
    {
      "type": "color",
      "id": "dot_active_color",
      "label": "Active dot color",
      "default": "#f575b4"
    }
  ],
  "presets": [
    {
      "name": "Video grid slider"
    }
  ]
}
{% endschema %}
